spring:
  application:
    name: message-service
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:20501
      fail-fast: true
      retry:
        # 最大重试次数
        max-attempts: 3