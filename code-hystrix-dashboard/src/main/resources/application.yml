spring:
  application:
    name: hystrix-dashboard
  profiles:
    active: dev
logging:
  file: logs/hystrix-dashboard.log
turbine:
  app-config: job
  aggregator:
    cluster-config: default
  cluster-name-expression: new String("default")
  
